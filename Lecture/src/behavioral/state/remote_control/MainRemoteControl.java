package behavioral.state.remote_control;

public class MainRemoteControl {
    public static void main(String[] args) {
        RemoteControl remoteControl = new RemoteControl();

        remoteControl.powerPress();
        remoteControl.powerPress();
        remoteControl.powerPress();
        remoteControl.powerPress();
    }
}
